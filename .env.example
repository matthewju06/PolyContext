# Copy this file to .env and replace the password before starting Docker Compose.
POSTGRES_USER=pearladmin
POSTGRES_PASSWORD=replace-with-a-local-password
POSTGRES_DB=pearlmetric_dev
POSTGRES_PORT=5432
